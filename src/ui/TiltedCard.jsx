import React from 'react';
import { TiltedCard as ReactBitsTiltedCard } from '@appletosolutions/reactbits';

const TiltedCard = (props) => {
    return <ReactBitsTiltedCard {...props} />;
};

export default TiltedCard;
