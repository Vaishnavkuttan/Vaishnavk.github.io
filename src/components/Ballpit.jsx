import React from 'react';
import { Ballpit as ReactBitsBallpit } from '@appletosolutions/reactbits';

const Ballpit = (props) => {
    return <ReactBitsBallpit {...props} />;
};

export default Ballpit;
