import { defineConfig } from 'jsrepo';

export default defineConfig({
    // configure where stuff comes from here
    registries: ['github/DavidHDev/react-bits'],
    // configure were stuff goes here
    paths: {},
});